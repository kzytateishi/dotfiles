# グローバル指示(全プロジェクト共通)

## 作業スタイル

- コミット・プッシュは明示的に頼まれたときだけ行う
- 破壊的な操作(force push、ファイル削除、DB 操作など)は実行前に必ず確認する
- 既存コードのスタイル(インデント、命名規則、コメントの密度)に合わせる

## 禁止事項

- `.env` や credentials などの秘密情報を読まない・コミットしない
- ライブラリのメジャーバージョン変更は事前に相談する
